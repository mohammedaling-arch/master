import { ZegoLogStrategy } from '../../zego.entity';
import { LogCache } from './index';
import { NetworkContext } from './network-context';
import { LogSender } from './log-sender';
export declare class LogSenderBuilder {
    private _netCtx;
    private _cache?;
    private _strategy;
    constructor(_netCtx: NetworkContext);
    get strategy(): ZegoLogStrategy;
    setCache(v: LogCache): void;
    setStrategy(v: ZegoLogStrategy): void;
    build(): LogSender;
}
