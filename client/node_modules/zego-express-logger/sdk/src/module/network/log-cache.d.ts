import { QueueCache } from '../common';
import { ConfigState } from '../common/config.state';
export declare class LogCache extends QueueCache {
    logCacheMax: number;
    private _configState;
    constructor(configState: ConfigState, logCacheMax?: number);
    size(): number;
    private _zgp_oneFrameMaxPacketLen;
    getOnePackMaxPacketLen(maxByteLength?: number): number;
    /**
     * 组装WebSocket的一帧数据报文中的detail_data。改为JSON数组转字符串
     * 一帧最长 64kB
     * @returns
     */
    getOneLogPacket(maxByteLength?: number): string | undefined;
    cacheLogPacket(logPacket: string): void;
    clear(): string[];
    removeALog(): void;
}
