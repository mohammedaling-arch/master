import { ENUM_NETWORK_STATE, ENUM_REMOTE_TYPE, IPStackMode, ZEGO_ENV } from '../../zego.entity';
export declare class ConfigState {
    url: string;
    logType?: ENUM_REMOTE_TYPE;
    env: ZEGO_ENV;
    useStrategy: boolean;
    appid: number;
    roomid: string;
    sessionid?: string;
    userid: string;
    version: string;
    token: string;
    deviceid: string;
    ipStackMode: IPStackMode;
    connectURLs: string[];
    networkState: ENUM_NETWORK_STATE;
}
